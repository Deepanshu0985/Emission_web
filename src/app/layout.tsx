import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Emissions Insights Dashboard | AI-Powered Analytics",
  description: "Visualize greenhouse gas emissions across sectors and years with an integrated AI chat assistant for data analysis and insights.",
  keywords: ["emissions", "greenhouse gas", "climate", "dashboard", "AI", "analytics"],
  authors: [{ name: "Stride Labs" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
